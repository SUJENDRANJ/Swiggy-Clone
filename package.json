{
  "name": "swiggy-clone",
  "version": "1.0.0",
  "description": "A: `Emmet` is the essential toolkit for web-developers. It allows you to `type shortcuts` that are then expanded into full pieces of code for writing `HTML and CSS`, based on an abbreviation structure most developers already use that expands into full-fledged HTML markup and CSS rules.",
  "main": "App.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [
    "Suje"
  ],
  "author": "SUJE",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
